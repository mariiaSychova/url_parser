WHITESPACE = _{ " " | "\t" | "\n" }
url = { scheme ~ "://" ~ domain ~ path? ~ query? ~ fragment? }
scheme = @{ ASCII_ALPHANUMERIC+ }
domain = @{ (!("/" | "?") ~ ANY)+ }
path = { "/" ~ (!("?" | "#") ~ ANY)* }
query = { "?" ~ (!"#" ~ ANY)* }
fragment = { "#" ~ ANY* }